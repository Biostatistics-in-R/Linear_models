---
title: "Tutorial"
output: learnr::tutorial
runtime: shiny_prerendered
---

```{r setup, include=FALSE}
library(learnr)
```

```{r mtcars-anova-kable, exercise=TRUE}
anova(lm(mpg ~ as.factor(gear), data = mtcars))
```

```{r mtcars-anova-raw, exercise=TRUE, paged.print=TRUE}
anova(lm(mpg ~ as.factor(gear), data = mtcars))
```